[package]
name = "fourk-harness"
version = "0.1.0"
edition = "2024"
publish = false

# Test harness for the FourK covenant: compiles contracts/fourk.sil with the
# Silverscript compiler and executes real signed transactions against the
# rusty-kaspa script engine. Same pattern as silverscript-lang's
# chess_apps_tests.rs. Dependency tags must match the silverscript workspace.

[lib]
path = "src/lib.rs"

[dependencies]

[dev-dependencies]
silverscript-lang = { path = "../../silverscript/silverscript-lang" }
kaspa-consensus-core = { git = "https://github.com/kaspanet/rusty-kaspa", tag = "v2.0.1" }
kaspa-txscript = { git = "https://github.com/kaspanet/rusty-kaspa", tag = "v2.0.1" }
kaspa-txscript-errors = { git = "https://github.com/kaspanet/rusty-kaspa", tag = "v2.0.1" }
secp256k1 = { version = "0.29.0", features = ["global-context", "rand-std"] }
serde_json = "1"

[workspace]
